# Lint and Fix

Run `make lint` and fix all identified issues.

1. Run `make lint` to see all problems
2. Run `make lint-fix` for auto-fixable issues
3. Fix remaining issues
4. Verify with `make lint` until clean
5. Run `make build` and `make test` to ensure nothing broke
