[project]
name = "tarsy-llm"
version = "0.1.0"
description = "TARSy LLM Service - gRPC wrapper for Gemini client"
requires-python = ">=3.13"
dependencies = [
    "grpcio>=1.81.1,<1.82.0",
    "grpcio-tools>=1.81.1,<1.82.0",
    "grpcio-health-checking>=1.81.1,<1.82.0",
    "google-genai>=2.18.1,<3.0.0",
    "pydantic>=2.13.4",
    "python-dotenv>=1.2.2",
    # LangChain multi-provider support
    "langchain-core>=1.6.0",
    "langchain-openai>=1.6.0",
    "langchain-anthropic>=1.6.0",
    "langchain-xai>=1.3.0",
    "langchain-google-genai>=4.3.4",
    "langchain-google-vertexai>=3.2.4",
]

[project.optional-dependencies]
test = [
    "pytest>=9.0.3",
    "pytest-asyncio>=1.3.0",
    "pytest-cov>=7.0.0",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
markers = [
    "unit: Unit tests (fast, no external dependencies)",
    "integration: Integration tests (may require external services)",
]

[tool.coverage.run]
source = ["llm"]
omit = ["*/tests/*", "*/llm_proto/*"]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "raise AssertionError",
    "raise NotImplementedError",
    "if __name__ == .__main__.:",
    "if TYPE_CHECKING:",
]

[tool.hatch.build.targets.wheel]
packages = ["llm"]

[tool.uv]
# langchain-openai 1.6 allows openai>=2.45,<4. Stay on 2.x so uv
# does not resolve openai 3.x (default HTTP client is httpx2).
constraint-dependencies = [
    "openai>=2.45.0,<3.0.0",
]
# langchain-google-vertexai pulls in langchain-tests as a runtime dep,
# which caps pytest<9. Override to allow our pytest>=9 requirement.
override-dependencies = [
    "pytest>=9.0.3",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
