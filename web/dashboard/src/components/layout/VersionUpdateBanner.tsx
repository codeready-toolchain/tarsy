/**
 * Banner shown when the dashboard version differs from the backend version,
 * prompting the user to refresh.
 *
 * Shell component — wired to real version monitoring in Phase 7.7.
 */
export function VersionUpdateBanner() {
  // Phase 7.7: useVersionMonitor() hook will drive this component.
  // For now, render nothing.
  return null;
}
