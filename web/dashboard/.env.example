# ─────────────────────────────────────────────────────────────
# Dashboard Environment Variables (.env.example)
# ─────────────────────────────────────────────────────────────
# Copy to .env and adjust values for your setup:
#   cp .env.example .env
#
# All VITE_* variables are embedded at build time by Vite.
# Non-VITE_ variables are only available in vite.config.ts.
# ─────────────────────────────────────────────────────────────

# ── Build-time ──────────────────────────────────────────────

# Application version shown in the dashboard UI.
# Set by CI in production builds; defaults to "dev" if unset.
VITE_APP_VERSION=dev

# ── API / WebSocket URLs (production only) ──────────────────

# Base URL for API calls. Leave empty for same-origin serving
# (the default when the Go backend serves the dashboard).
# Only set if the dashboard is hosted on a different origin.
# VITE_API_BASE_URL=https://tarsy.example.com

# WebSocket base URL. In production, derived from page location
# automatically (ws:/wss: based on protocol). Override only if
# the WebSocket endpoint lives on a different host/port.
# VITE_WS_BASE_URL=wss://tarsy.example.com

# ── Vite dev server ────────────────────────────────────────

# Host and port for `npm run dev`. Defaults: localhost:5173.
# VITE_DEV_SERVER_HOST=localhost
# VITE_DEV_SERVER_PORT=5173

# ── Container mode (podman-compose / oauth2-proxy) ─────────

# Set these when running the dashboard dev server inside a
# container alongside oauth2-proxy (e.g., podman-compose).
# VITE_PROXY_TARGET_HTTP=http://oauth2-proxy:4180
# VITE_PROXY_HOST_HEADER=localhost:4180
