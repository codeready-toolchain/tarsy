# OpenShift Deployment Configuration
# Copy to openshift.env and fill in your values:
#   cp openshift.env.example openshift.env
#
# ROUTE_HOST is auto-detected from the cluster domain (via oc) — no need to
# set it unless auto-detection fails or you want a custom hostname.
# Auto-detected value: tarsy.<cluster-apps-domain>
# ROUTE_HOST=tarsy.apps.cluster.example.com

# GitHub org/team restriction for oauth2-proxy
GITHUB_ORG=your-org
GITHUB_TEAM=your-team
